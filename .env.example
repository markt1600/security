# Vercel project environment variables. Never commit real credentials.
# Generate a unique random secret of at least 32 characters.
# Set the SAME value in both the security and mainpage Vercel projects.
SECURITY_SSO_SECRET=
# Must belong to a PRIVATE Blob store dedicated to this camera project.
BLOB_READ_WRITE_TOKEN=
